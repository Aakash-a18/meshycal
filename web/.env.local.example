# Copy to .env.local. Per CLAUDE.md rule 4: no hardcoded URLs.

# Base URL of the meshycal.api FastAPI app.
# In dev, the api runs at http://localhost:8000 (see meshycal/api/README).
NEXT_PUBLIC_MESHYCAL_API_URL=http://localhost:8000
